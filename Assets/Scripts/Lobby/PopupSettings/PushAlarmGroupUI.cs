using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 푸쉬알람 관리. Firebase API와 연계해서 구현해야할듯. 기획에 따라 구현여부 결정하기. 
/// </summary>

public class PushAlarmGroupUI : MonoBehaviour
{
    [SerializeField] private SwitchUI switchUI;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
